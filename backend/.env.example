# Server Settings
PORT=5000
JWT_SECRET=personal_finance_super_secret_key_12345

# Database Configuration
DATABASE_URL=postgresql://username:password@hostname:5432/dbname?sslmode=require

# AI Configuration (Optional)
# If no keys are provided, the chatbot will use the Rule-Based Fallback Parser
AI_PROVIDER=gemini
GEMINI_API_KEY=
GROK_API_KEY=
